services:
  - type: web
    name: concurrent-job-processor
    runtime: go
    plan: free
    buildCommand: go build -o server ./cmd/server
    startCommand: ./server
    envVars:
      - key: GIN_MODE
        value: release
      - key: DATABASE_URL
        sync: false
